<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="AntrolabsHD"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntrolabsHD">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Instrumento Pericial V4 (Compose nativo): Explorar/Observar/Triangular/Fijar.
             No es punto de entrada de la app; MainActivity (Radar por WebView) sigue
             siendo el launcher. -->
        <activity
            android:name=".instrumento.InstrumentoActivity"
            android:exported="false" />

    </application>

</manifest>
