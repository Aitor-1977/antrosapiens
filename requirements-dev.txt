# Dependencias de desarrollo y test. NO se instalan en el deploy serverless.
# Uso local: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0,<9.0
# Conector YouTube (se invoca el binario yt-dlp por subprocess). Es pesado y no
# funciona en serverless; por eso vive aquí y no en el runtime de producción.
yt-dlp>=2024.1
