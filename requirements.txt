# hd-prospector — motor de extracción de evidencia (runtime de PRODUCCIÓN / Vercel)
# Solo lo que necesita la API serverless. Dev y test → requirements-dev.txt
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
# httpx cubre también el consumo REST de NVIDIA NIM (síntesis estructural Capa 19,
# endpoint compatible con OpenAI: POST /v1/chat/completions). No se necesita el
# SDK openai: evitamos pydantic-core en el runtime serverless.
httpx>=0.27,<1.0
feedparser>=6.0,<7.0
apscheduler>=3.10,<4.0
python-dateutil>=2.9,<3.0
# PostgreSQL (producción). SQLite (dev/tests) usa la stdlib, sin paquete.
psycopg[binary]>=3.1,<4.0
# BeautifulSoup: incluido pero de uso restringido (solo si es indispensable).
beautifulsoup4>=4.12,<5.0
python-dotenv>=1.0,<2.0
